# Kuvryn Sync e2e fixture: a pre-sync hook that must finish before anything else.
apiVersion: batch/v1
kind: Job
metadata:
  name: kuvryn-sync-e2e-migrate
  namespace: kuvryn-sync-e2e-staged
  annotations:
    sync.kuvryn.io/hook: pre-sync
spec:
  backoffLimit: 0
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: migrate
          image: busybox:1.36
          command: ["sh", "-c", "echo migrating; sleep 5"]
